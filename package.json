{
  "name": "pomogarium",
  "version": "1.0.0",
  "private": true,
  "description": "Лендинг репетиторского центра «Помогариум» — React + Vite, деплой на Vercel",
  "workspaces": [
    "client"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run dev --workspace client",
    "build": "npm run build --workspace client",
    "preview": "npm run preview --workspace client",
    "typecheck": "npm run typecheck --workspace client"
  }
}
